# for base interface get the referent from base.graphql
type Book implements BaseEntity {
    id: ID!
    createdAt: LocalDateTime!
    updatedAt: LocalDateTime!
    status: GStatus!
}

# Root
extend type Query {
    getEntity: Book!
}